package uk.org.nbn.nbnv.importer.darwin

import uk.org.nbn.nbnv.importer.testing.BaseFunSuite
import uk.org.nbn.nbnv.importer.Options
import uk.org.nbn.nbnv.importer.utility.ResourceLoader

class ReaderSuite extends BaseFunSuite with ResourceLoader {

  test("an empty value should be an empty string, not null") {

//    val archivePath = resource("/archives/valid.zip")
//    val options = Options(archivePath = archivePath.getFile, tempDir = tempDir, whatIf = true)
//    val am = new ArchiveManager()
  }
}
