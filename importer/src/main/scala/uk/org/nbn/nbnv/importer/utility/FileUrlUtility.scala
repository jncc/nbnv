package uk.org.nbn.nbnv.importer.utility

import java.net.URL

object FileUrlUtility {

  /// I can't see how to do this correctly with the Java libraries right now.
  def javaFileUrlToWindowsUncPath(url: URL) = {
    "hello"
  }
}
